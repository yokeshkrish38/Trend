apiVersion: apps/v1
kind: Deployment
metadata:
  name: trend-app
  namespace: app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trend
  template:
    metadata:
      labels:
        app: trend
    spec:
      containers:
      - name: trend
        image: krish96/trend-app:latest
        ports:
        - containerPort: 80

